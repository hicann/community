# gauss-splat仓测试报告

## 1. 概述

描述本报告覆盖的范围，说明新增的特性清单以及解决问题列表，明确所需的测试活动

本报告为gauss-splat项目开源测试报告，在该阶段完成了基于 CANN 9.0.0 开源包的能力测试，覆盖所有算子样例代码及文档，测试过程中执行覆盖率达到100%。

### 新增特性清单

| 序号 | 特性名称                                    | 特性描述                                                     |
| ---- | ------------------------------------------- | ------------------------------------------------------------ |
| 1    | 基于 AscendC 的 PyTorch 自定义算子库        | 使用 方式实现高性能 NPU 算子 |
| 2    | Python wheel 包编译与安装                  | 编译为 wheel 包供直接 pip 安装使用，支持 Python 3.7+ |
| 3    | 多架构算子支持                             | 支持 Atlas A2/A3（arch22）芯片架构       |
| 4    | gauss-splat 算子实现                      | 支持10个3D Gaussian Splatting核心算子，包括球谐函数、协方差计算、投影、排序、筛选等                    |

### 解决问题列表

不涉及（首个版本发布，无历史问题）

## 2. 版本测试信息

**硬件和版本要求**

产品型号： Atlas A2 / Atlas A3

操作系统：覆盖以下测试因子组合

|      | 因子名称 | 因子取值               |
| ---- | -------- | ---------------------- |
| 1    | 硬件       | Atlas A2训练推理系列产品/Atlas A3训练推理系列产品    |
| 2    | 操作系统 | EulerOS 2.0 (aarch64) / Ubuntu 24.04 (x86_64)            |
| 3    | CANN版本     | 8.5.0/9.0.0/9.1.0-beta.3 |
| 4    | Python     | 	>=3.7 |
| 5    | PyTorch     | 	配套CANN版本，>=2.7.1 |
| 6    | torch_npu     | 配套torch版本 ，>=7.3.0 |

## 3. Readme内容评估

本节描述Readme文档核心验证要点及结果，覆盖文档内容完整性、文档内容正确性等维度

Readme内容评估：通过；

| 验证项         | 验证内容                                                     |
| -------------- | ------------------------------------------------------------ |
| 文档内容完整性 | 1、提供算子领域公共知识/特性说明等内容<br />2、提供开源仓完整算子清单，说明对应算子实现逻辑、参数、约束等内容<br/>3、提供算子开发实例说明（docs/kernel/*）<br/>4、每个算子提供了算子调用实例说明（docs/gauss_splat_api.md）<br/>5、提供快速开始、项目结构、Python接口、底层算子接口（ACLNN）<br/>6、提供贡献指南（CONTRIBUTING.md）、安全声明（SECURITY.md）、许可证（LICENSE） |
| 文档内容正确性 | 1、仓中介绍内容和算子实际实现一致，gauss-splat算子接口、数据类型、芯片支持范围均与代码一致<br/>2、目录排版合理，逻辑清晰，总览和各子模块映射易获取，跳转准确<br/>3、编译命令、安装命令、测试命令经实际验证可正确执行 |

## 4. 编译功能验证

本节描述编译功能核心验证要点及结果，覆盖通用性、易用性、高效性等维度

编译功能评估：通过；

| 验证项     | 验证内容                                                     |
| ---------- | ------------------------------------------------------------ |
| 编译易用性 | 编译命令直观易操作，bash build.sh直接编译，支持通过--python指定python版本，日志无 ERROR、非法指令可有效拦截并提示 |
| 编译高效性 | CMake 构建默认并行编译（`--parallel $(nproc)`），单算子编译时长在合理范围内，实测数据有效 |
| 出包全面性 | 正常出包（wheel 包），包含算子动态库（.so）及 Python 包文件，产物为 `dist/gauss_splat-1.0.0*.whl` |
| 包大小合理 | wheel 包大小合理 |

## 5. 安装包功能验证

本节描述安装包操作验证，覆盖安装路径、CANN 适配及卸载功能

安装包功能评估：通过；

| 验证项          | 验证内容                                                 |
| --------------- | -------------------------------------------------------- |
| 算子包安装      | wheel 包通过 `pip install` 安装，支持 `--force-reinstall --no-deps` 选项 |
| CANN 包路径适配 | 兼容 CANN 默认及自定义安装路径，cmake 自动检测 Torch/Torch NPU/Ascend 路径 |
| 卸载功能        | wheel 包可通过 `pip uninstall gauss_splat` 正常卸载，卸载后无残留 |

## 6. 算子UT功能验证

本节描述算子测试核心要求，覆盖测试范围、日志

测试功能评估：通过；

| 验证项     | 验证内容                                         |
| ---------- | ------------------------------------------------ |
| 算子UT功能 | 对应子仓全量 UT 测试，日志信息无 ERROR           |

当前仓内已实现 10个算子，包含10个 UT 测试用例：

### 6.1 接口注册测试

| 序号 | 测试用例名称            | 测试内容                             | 结果 |
| ---- | ----------------------- | ------------------------------------ | ---- |
| 1    | test_calc_render_bwd | 计算渲染反向传播功能和精度| PASS |
| 2    | test_calc_render_fwd | 前向渲染功能和精度 | PASS |
| 3    | test_flash_gaussian_build_mask | 根据高斯球位置、透明度等参数构建tile掩码功能和精度 | PASS |
| 4    | test_gaussian_filter | 根据视锥体参数对高斯球进行筛选，剔除不在可见范围内的高斯球功能和精度 | PASS |
| 5    | test_gaussian_sort | 高斯球深度排序功能和精度 | PASS |
| 6    | test_projection_3dgs_bwd | 3D高斯球投影到2D图像平面反向传播功能和精度 | PASS |
| 7    | test_projection_3dgs_forward | 3D高斯球投影到2D图像平面，计算投影后的位置、深度、锥形参数功能和精度 | PASS |
| 8    | test_spherical_harmonics_bwd | 计算球谐函数的反向传播功能和精度 | PASS |
| 9    | test_spherical_harmonics | 计算球谐函数的前向传播功能和精度 | PASS |
| 10   | test_rasterization.py | 渲染3D高斯点云到2D图像功能和精度 | PASS |


## 7. 环境兼容性验证

本节描述环境部署兼容性，覆盖三方依赖、OS、CANN 包及形态适配

兼容性评估：通过；

| 验证项         | 验证内容                                                     |
| -------------- | ------------------------------------------------------------ |
| 三方依赖兼容性 | 空环境按 README 安装依赖可正常编译；兼容 requirements.txt 中声明的依赖版本范围 |
| OS 兼容性      | 覆盖对应设备形态的 Ubuntu x86、Euler ARM64 版本，均可正常编译 |
| CANN 包兼容性  | 兼容配套 CANN 版本（>=8.5.0），cmake 自动检测 Ascend 工具链路径，编译无报错 |
| 形态兼容性     | 标准 host+device 两种场景，均可正常编译开源仓        |
| 芯片架构兼容性 | 支持 ascend910b（arch22）、ascend910_93（arch22）两种 SoC 类型 |

## 8. 测试结论

测试结论：通过。
gauss-splat 仓基于 CANN 8.5.0 开源包完成全量测试，所有验证项均通过，无遗留缺陷。
gauss-splat 仓基于 CANN 9.0.0 开源包完成全量测试，所有验证项均通过，无遗留缺陷。
gauss-splat 仓基于 CANN 9.1.0-beta.3 开源包完成全量测试，所有验证项均通过，无遗留缺陷。

| 评估维度       | 评估结果                                    |
| -------------- | ------------------------------------------- |
| 测试用例总数   | **10** |
| 测试执行通过率 | **100%**（10/10 全部通过）                  |
| 测试结论       | **通过**                                    |


## 9. 遗留问题和关键风险

### 9.1 遗留问题统计

不涉及

### 9.2 遗留问题列表

不涉及

## 10. 附件

不涉及